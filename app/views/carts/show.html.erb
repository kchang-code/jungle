<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <div class="panel panel-default items">
    <% if enhanced_cart.length == 0 %>
    <div>
    <h2 >The Cart is empty!</h2>
    <p >Shop around and add some new products to your cart</p>
      <div>
        <%= link_to 'Home', :root, class: 'btn btn-info' %>
      </div>
      </div>
    <% else %>
      <%= render 'cart_items', @enhanced_cart %>
    <% end %>

  </div>

  <div class="row">
    <div class="col-xs-12">
      <%= form_tag orders_path do %>
        <script
          src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-amount="<%= cart_subtotal_cents %>"
          data-name="Jungle"
          data-description="Khurram Virani's Jungle Order"
          data-locale="auto"
          data-email="kvirani@gmail.com"
          data-currency="cad">
        </script>
      <% end %>
    </div>
  </div>

</section>
